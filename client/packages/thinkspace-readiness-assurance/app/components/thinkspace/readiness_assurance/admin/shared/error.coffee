import ember from 'ember'
import base  from 'thinkspace-readiness-assurance/base/admin/component'

export default base.extend

  valid:  true  # default to true if only called with an errors array 
  errors: null
